# ---------------------------------------------
# PneumoDetect Requirements (Cross-platform)
# ---------------------------------------------
#
# Tested with Python 3.11. PyTorch wheels for the pinned macOS x86_64
# versions are not available for Python 3.13.

# Core scientific stack
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
tqdm==4.66.5
matplotlib==3.9.2
seaborn==0.13.2
opencv-python==4.10.0.84
Pillow==12.3.0
pydicom==2.4.5

# Machine learning (CPU default; platform-aware pins)
# - macOS x86_64 wheels stop at torch 2.2.x; arm64/Linux can use 2.3.x+
torch==2.13.0; platform_system == "Darwin" and platform_machine == "x86_64"
torchvision==0.17.2; platform_system == "Darwin" and platform_machine == "x86_64"
torch>=2.3.0,<2.5.0; platform_system != "Darwin" or platform_machine != "x86_64"
torchvision>=0.18.0,<0.20.0; platform_system != "Darwin" or platform_machine != "x86_64"

# Flask web app
Flask==3.1.3
Werkzeug==3.1.6
jinja2==3.1.6
itsdangerous==2.2.0
gunicorn==23.0.0

# Testing & CI
pytest==9.0.3
pytest-cov==5.0.0
flake8==7.1.1
